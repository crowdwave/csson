{
  "name": "csson",
  "version": "1.0.0",
  "description": "CSSON reader for the browser — walks the built-in CSSOM (cssRules) of a loaded .csson stylesheet and returns canonical JSON. Zero dependencies; byte-identical to the lexbor core across Chrome and Firefox.",
  "type": "module",
  "main": "./dist/csson.js",
  "types": "./dist/csson.d.ts",
  "exports": { ".": { "types": "./dist/csson.d.ts", "import": "./dist/csson.js" } },
  "files": ["dist", "src", "README.md"],
  "scripts": { "build": "tsc -p tsconfig.json" },
  "keywords": ["css", "csson", "config", "structured-data", "cssom", "browser"],
  "license": "MIT",
  "sideEffects": false,
  "devDependencies": { "typescript": "^5.5.0" }
}
