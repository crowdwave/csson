# Build output (CMake / lexbor clone / old ad-hoc build)
core/build*/
core/obj/
core/csson
core/libcsson.a
*.o
*.a

# Scratch (never committed; recreated on demand)
tmp/

# Node / TypeScript
node_modules/

# Conformance harness scratch
conformance/v1/.verify-out/
conformance/**/.sec*/
conformance/**/.edits*/

# Editor / OS
.DS_Store
*.swp

# WASM build scratch (quickjs clone + bundle header); the final csson.wasm is committed
bindings/wasm/build/

# Local maintainer notes (machine-specific gh/git workarounds; not published)
docs/maintainers/

# Binding caches (the prebuilt dist/, csson.wasm and libcsson.so ARE committed)
bindings/python/__pycache__/
bindings/python/*.egg-info/

# Local scratch TODO (not published)
TODO.md
